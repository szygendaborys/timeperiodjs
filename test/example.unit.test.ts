import sinon from 'sinon';
import 'mocha';

describe('Example unit tests', () => {

    // let phoneNumber = '12345678';

    beforeEach(() => {
        // sinon.restore();
    });

    afterEach(() => {
        sinon.restore();
    })

    // it('Should send the token', async() => {
    // const sendStub = sinon.stub(Token, 'updateOne');
    // const canSendToken = sinon.stub(handler, 'canUserSendToken');

    // tokenStub.withArgs(asd).resolves({
    //     updatedAt: new Date(2000,1,1),
    //     createdAt: new Date(2000,1,1),
    // })

    // canSendToken.withArgs(asd).resolves(undefined);

    // const response = await handler.sendToken(asd,phoneNumber)

    // expect(response).to.be.undefined;
    // sinon.assert.callCount(sendStub, 1);

    // });

});